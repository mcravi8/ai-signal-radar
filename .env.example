# Optional: improves GitHub public API rate limits. Never commit a real token.
AI_RADAR_GITHUB_TOKEN=

# Local-only path to a sanitized newsletter export. Raw mailbox exports are rejected.
AI_RADAR_EMAIL_EXPORT=
